<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.software.tongji.easygo">

    <!-- 允许程序打开网络套接字 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许程序设置内置sd卡的写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许程序获取网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许程序访问WiFi网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 允许程序读写手机状态和身份 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 允许程序访问CellID或WiFi热点来获取粗略的位置 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="454bde9b9ddd414955ceee80012e27bc" />

        <activity android:name=".MainActivity" >

        </activity>
        <activity
            android:name=".login.LoginActivity"
            android:theme="@style/AppNoBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".navigation.NavigationActivity"
            android:theme="@style/AppNoBarTheme" />

        <activity
            android:name=".details.DetailsActivity"
            android:theme="@style/AppNoBarTheme" />
        <activity
            android:name=".newschedule.NewScheduleActivity"
            android:theme="@style/AppNoBarTheme" />
        <activity
            android:name=".journaledit.JournalEditActivity"
            android:theme="@style/AppNoBarTheme" />
        <activity
            android:name=".journal.JournalShowActivity"
            android:theme="@style/AppNoBarTheme" />
        <activity android:name=".inputTips.InputTipsActivity"
            android:theme="@style/AppNoBarTheme">
        </activity>
    </application>

</manifest>